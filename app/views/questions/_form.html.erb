<!--<%= simple_form_for question,
  html: { data: { controller: "reset-form", action: "turbo:submit-end->reset-form#reset" }} do |f| %>
  <div class="input-group mb-3 w-100">
    <div class="form-floating">
      <%= f.input :user_question, label: false, wrapper: false %>
      <%= f.label :user_question, "Ask Me anything" %>
    </div>
    <%= f.submit "Ask", id: "new-question-button", class: "btn btn-primary" %>
  </div>
<% end %>-->

<%= simple_form_for question,
   html: { data: { controller: "reset-form", action: "keydown->reset-form#submitOnEnter turbo:submit-end->reset-form#reset" }} do |f| %>
  <div class="form-group">
    <%= f.input :user_question,
                label: false,
                input_html: {
                  class: 'chat-input',
                  rows: 4
                } %>
    <%= f.submit "Ask", class: "btn-medieval" %>
  </div>
<% end %>
